#include <stdio.h>
#include <stdlib.h>


cell_t liste_des_diviseurs(unsigned int n)
{
  return NULL;
}

cell_t elements_communs(cell_t *a, cell_t *b)
{
	return NULL;
}


unsigned int plus_grand_element(cell_t *root)
{
  return 0;
}

unsigned int pgcd_inefficace(unsigned int a, unsigned int b)
{
  return 0;
}

int main(void)
{
  int res = pgcd_inefficace(15, 5);
  printf("%u\n", res);
  return 0;
}
